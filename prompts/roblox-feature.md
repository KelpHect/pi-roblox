---
description: Implement and prove a Roblox feature through Rojo and Studio
---
Implement this Roblox feature: $@

Use the `roblox-development` skill. Establish environment health and ownership first. Search and inspect only relevant source/instances, then present a compact implementation plan. Keep authoritative code and reproducible structure in Rojo-owned files; use `roblox_mutate` only for Studio-owned content. Use fresh SHA-256 preconditions, checkpoints, and a dry run for risky batches. Run the appropriate validation profile and a deterministic Studio scenario for runtime or visual behavior. Finish with exact changed paths, checkpoint IDs, sync proof, validation/playtest/console/screenshot evidence, and remaining limitations.
