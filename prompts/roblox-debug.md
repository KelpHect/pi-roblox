---
description: Diagnose a Roblox bug with source ownership, runtime evidence, and a verified repair
---
Debug this Roblox problem: $@

Use the `roblox-development` skill. Start with `roblox_status`; use `roblox_doctor` when integration health is uncertain. Reproduce the failure before changing code when practical, capture the same console/screenshot/scenario evidence that demonstrates it, and map Studio stack paths back to source. Inspect immediately before mutation and use fresh SHA-256 preconditions. Respect the Rojo/Studio ownership boundary. Run deterministic checks and replay the same reproduction after the change. Finish with root cause, exact changes, before/after evidence, checkpoint IDs, and any unproven assumptions.
